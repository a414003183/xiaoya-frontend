/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Zentao API
 * 自研项目管理平台 API。所有时间 ISO-8601 带时区；信封见 components。
 * 列表 DSL（03 §3）：page/limit/sort/filters[x]/q；游标时间线：limit/beforeId。
 *
 * OpenAPI spec version: 0.2.0
 */
import type { RouteEntryStatus } from './routeEntryStatus';

/**
 * 一条页面路由：path 是数据（可被管理端改），component 是代码实现
 */
export interface RouteEntry {
  /** 路由路径（管理端可改；改了之后前端按新路径挂载同一个组件） */
  path: string;
  /** 页面实现（组件名）：前端从生成的组件表里按名取 */
  component: string;
  /** i18n 键（内置页面）或字面文案（管理端覆盖过） */
  title: string;
  /**
     * 进入该页所需权限码（前端据此渲染 403 页；真鉴权在后端）
     * @nullable
     */
  perm?: string | null;
  /**
     * 隐藏页的菜单高亮目标（NULL = 不是隐藏页或未声明）
     * @nullable
     */
  activeMenu?: string | null;
  /** disabled 只让菜单项不进侧栏，页面仍可直达（与 MenuNode.status 同义） */
  status: RouteEntryStatus;
  /** 是否隐藏页（不进侧栏） */
  hidden: boolean;
}
